# SpecScore Repo Config Schema: https://specscore.md/repo-config

project:
    title: ext-sizeus
    host: github.com
    org: sneat-co
    repo: ext-sizeus
publication:
    default:
        actions:
            - stage
            - commit
            - push
    push:
        allow_branches:
            - main
        deny_branches: []
